<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:projet="clr-namespace:Projet"
             xmlns:forms="clr-namespace:Microcharts.Forms;assembly=Microcharts.Forms"
             x:Class="Projet.ProjectGraphPage"
             x:DataType="projet:ProjectGraphViewModel">
    <ContentPage.Content>
        <StackLayout VerticalOptions="Center"
                     HorizontalOptions="Center"
                     Margin="20, 10">
            <Label Text="Time spent on a project (minutes)"
                   VerticalOptions="CenterAndExpand"
                   HorizontalOptions="CenterAndExpand"
                   FontSize="30"/>
            <ActivityIndicator IsRunning="{Binding Working}" />
            <forms:ChartView HeightRequest="300" WidthRequest="300"
                             Chart="{Binding ProjectChart}"></forms:ChartView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>